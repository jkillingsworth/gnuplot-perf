#include <string>
#include <vector>

//-------------------------------------------------------------------------------------------------

namespace chart
{
    std::string create_plot(const std::string& outputPath, const std::vector<std::string>& lines, int index);
}
